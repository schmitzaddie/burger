<h1>BURGERS</h1>

<h2>What kind of burger sounds good?</h2>

<ul>
    {{#each burgers}}
        {{#unless devour}}
            {{burger_name}}
        {{/unless}}
    {{/each}}
</ul>

<h2>Burgers that have already been devoured...</h2>

<ul>
    {{#each burgers}}
        {{#if devour}}
            {{burger_name}}
        {{/if}}
    {{/each}}
</ul>

<h2>Add a burger?</h2>
<form class="create-form">
    <div class="form-group">
        <label for="ca">Burger Name:</label>
        <input type="text" id="ca" burger_name="burger_name">
    </div>

    <div class="form-group">
        <label for="devour">Devour?</label>
        <br>
        <input type="radio" name="devour" value="1">Not yet!
        <br>
        <input type="radio" name="devour" value="0">Devour!
    </div>

    <button type="submit">Add!</button>
</form>